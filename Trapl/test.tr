Qux: struct { }
Foo::{ Qux: struct { } }
Foo::Bar::{ Qux: struct { } }


test: fn(x: Qux, y: _::Foo::Qux) -> (_::Qux, Foo::Bar::Qux)
{
	test(test(x), y);
	x = 0;
	if x
		{ test(y, x) }
	else
		{ test(x, y) }
}
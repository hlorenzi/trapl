struct Test
{
	x: Int,
	y: &Engine::Lib::Test,
	z: (Test, Global::Int),
	w: &'test Float32
}


fn test(x: Int, y: &'a MyModule::Test::Tester) -> (Int8, Int16)
{
	x;
	let z;
	let z: Int;
	let z = 0;
	let z: Int = 0;
	let _;
	let _: Float32;
	let _ = 0;
	let _: Float32 = 0;

	while x	{ do() };
	return w;
}
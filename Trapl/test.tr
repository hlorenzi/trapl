Foo: struct { }
Bar: struct { }
Qux: struct { }


qux_for_foo: fn(foo: Foo) -> Qux
{

}


test: fn() -> ()
{
	let foo: Foo;
	let bar: Bar;
	let qux: Qux;

	foo = (bar);
	foo = foo;
	foo = bar;
	foo = test;
	foo = test();
	foo = qux_for_foo(foo);
	qux = qux_for_foo(foo);

	test();
	foo();
	qux_for_foo();
	qux_for_foo(foo);
	qux_for_foo(bar);

	let if_result = if foo { bar } else { qux };
}
fn test(z: Int) -> Bool
{
	let x;
	let y;
	let w = add(x, y);

	if x
		{ add(x, x) }
	else
		{ add(y, y) }
}


fn add<Int>(x: Int, y: Int) -> Int
{
	let a = add;
	a(x, y)
}


fn add<Bool>(x: Bool, y: Bool) -> Bool
{
	add(x, y)
}